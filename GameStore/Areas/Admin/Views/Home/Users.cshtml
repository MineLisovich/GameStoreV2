@model GameStore.Models.UserViewModel
<div class="container">
  
           
          
         
         <div class = "single-shop-product">
             <a class="add_to_cart_button"asp-area="Admin" asp-controller="Users" asp-action="Create">Добавить Нового пользователя</a>
         </div>
           <form method="get">
            <div class="form-group">
                <input name="name" class="form-control" placeholder="Введите логин пользователя"/>
            </div>
        </form>
            <table class="tableBasket">
               <thead>
                 <tr>
                    <td><b>ID:</b></td>
                    <td><b>Логин:</b></td>
                    <td><b>Email:</b></td>
                    
                    <td><b>Управление</b></td>
                 </tr>
             </thead>
             <tbody> 
             @if (Model.user.Count() > 0)
             {
                 @foreach (var obj in Model.user)
                 {
                     <form asp-area="Admin" asp-controller="Users" asp-action="Delete" asp-route-id="@obj.Id" method="post">
                     <tr>
                         <td>@obj.Id</td>
                         <td>@obj.UserName</td>
                         <td>@obj.Email</td>
                        
                         <td>
                             <a class="control_buttons"asp-area="Admin" asp-controller="Users" asp-action="Edit" asp-route-id=@obj.Id >Изменить</a>
                             <button class="control_buttons" type="submit" value="Удалить" >Удалить</button> 
                             
                         </td>
                     </tr>
                     </form>

                    
                 }
             }          
            </tbody>
            </table>
 </div>
 
